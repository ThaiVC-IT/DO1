{
  "variables": {
    "aws_access_key": "",
    "aws_secret_key": "",
    "aws_subnet_id": ""
  },
  "builders": [
    {
      "type": "amazon-ebs",
      "access_key": "{{user `aws_access_key`}}",
      "secret_key": "{{user `aws_secret_key`}}",
      "subnet_id": "{{user `aws_subnet_id`}}",
      "region": "us-east-1",
      "instance_type": "t2.micro",
      "ami_name": "bookapp-{{timestamps}}",
      "source_ami_filter": {
        "filters": {
        }
      }
    }
  ]
}
